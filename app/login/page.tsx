export default function LoginPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-md items-center px-6">
      <section>
        <h1 className="text-3xl font-semibold">Platform sign in</h1>
        <p className="mt-3 text-stone-600">
          Platform-admin authentication is connected when Phase 1 APIs are available.
        </p>
      </section>
    </main>
  );
}
